{
  "name": "server",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "prod src/public/script.js /dist/bundle.js -p"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.1",
    "dotenv": "^16.0.0",
    "express": "^4.17.3"
  },
  "description": "Weather App created in Vanilla Javascript, HTML, CSS(SASS). And the backend built in Node.js and Express.js and produced with Gulp.js",
  "devDependencies": {
    "gulp": "^4.0.2"
  }
}
